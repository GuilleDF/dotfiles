#include "binomial.h"
#include <stdio.h>
#include <unistd.h>

int main() {
  double probs[128];
  memset(probs, 0, 128);
  binomial(probs, 128, 0.5);
  for(int i=0; i<128; i++) {
    printf("%f ",probs[i]);
  }
}
