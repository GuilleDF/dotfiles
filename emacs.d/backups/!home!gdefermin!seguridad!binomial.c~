#include <math.h>

int fact(int n) {
  if(n==0) return 1;
  for(int i=2; i<n; i++)
    n *= i;
  return n;
}

void binomial(double *probs, int n, double p) {
  for(int i=0; i<n; i++) {
    probs[i] = fact(n)/(fact(i)*fact(n-i)) * pow(p,(double)i) * pow(1-p,(double)i); 
  }
}
